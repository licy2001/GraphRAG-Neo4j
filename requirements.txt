# =============================================================================
# fan-ms-graphrag-neo4j  Python 3.10–3.12
#
# Usage:
#   pip install -r requirements.txt          # 只装核心依赖
#   pip install -r requirements.txt -e .     # 同时安装本项目（可编辑模式）
# =============================================================================

# --- Core (pyproject.toml) ---
graphrag>=3.1.0
neo4j>=5.26.0
pandas>=2.2.0
pyarrow>=15.0.0
python-dotenv>=1.0.1
pyyaml>=6.0.1
typer>=0.12.0
rich>=13.7.0

# --- Docs extras ---
pypdf>=6.0.0
python-docx>=1.1.2

# --- Dev extras ---
pytest>=8.0.0
ruff>=0.6.0
